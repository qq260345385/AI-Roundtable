# Real Meeting Example

这里用于保存脱敏后的真实模型会议示例。

请勿提交任何 API key、账号信息、内部地址、私有提示词、私密问题或敏感输出。

当前文件只是记录模板，不代表已经完成真实 provider 测试。真实会议结果请从页面的“复制 Markdown”得到，并在提交前脱敏。

## 建议格式

```markdown
# AI Roundtable Real Meeting Example

## 基本信息

- 运行模式：real
- 启用模型：OpenAI / example-model, DeepSeek / example-model
- 记录时间：YYYY-MM-DD

## 输入问题

这里填写脱敏后的问题。

## 会议纪要

这里粘贴“复制 Markdown”得到的脱敏会议结果。

如果真实会议中出现部分 provider 调用失败，当前系统会在会议纪要中加入“模型调用失败记录”。该区块会包含 provider、model、阶段、简短原因和建议。请保留脱敏后的失败记录，但不要伪造具体 provider 失败结果。

## 观察结论

- 模型差异：
- 是否保留分歧：
- 少数派观点：
- 风险点：
- 下一步：
```

提交前请再次检查：

- 没有 API key
- 没有账号信息
- 没有内部地址
- 没有 Authorization 或 Bearer token
- 没有第三方完整错误体
- 没有私密问题
- 没有敏感输出
